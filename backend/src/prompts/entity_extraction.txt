Bạn là chuyên gia xây dựng Knowledge Graph từ tài liệu (tiếng Việt hoặc tiếng Anh).

Nhiệm vụ: Trích xuất các thực thể có ý nghĩa từ đoạn văn dưới đây.

{domain_hint_block}LOẠI THỰC THỂ ưu tiên (chọn loại phù hợp nhất; có thể đề xuất loại mới snake_case nếu nội dung yêu cầu):
{entity_types_block}
LUẬT NGHIÊM NGẶT:
1. CHỈ trích xuất thực thể CÓ Ý NGHĨA — không trích đại từ, giới từ, liên từ, từ thông thường.
2. Dùng dạng CHUẨN (canonical form), giữ nguyên dấu tiếng Việt nếu nguồn là tiếng Việt.
3. Mỗi thực thể: 2–60 ký tự, tối đa 6 từ.
4. KHÔNG trích: số trang, số hình, tiêu đề mục, URL, đường dẫn file.
5. Đặt `type` bằng tên ngắn snake_case tiếng Anh (vd. "person", "regulation", "court_case").
6. `confidence` ∈ [0.0, 1.0]; chỉ giữ thực thể bạn thực sự chắc chắn (≥ 0.5).
7. Nếu không có thực thể nào hợp lệ, trả về mảng rỗng [].

ĐỊNH DẠNG ĐẦU RA: chỉ JSON array, KHÔNG có giải thích, KHÔNG có markdown fence.
Schema mỗi phần tử: {{"name": str, "type": str, "confidence": float}}

FEW-SHOT EXAMPLES:
{few_shots_block}
Bây giờ hãy trích xuất từ đoạn văn sau:

TEXT:
{text}

OUTPUT (JSON array only):
